/**

   IQ: NodeList vs HTMLCollection

       NodeList:

         1. represnts the collecton nodes
         2. NodeList is returned by querySelectorAl()
         3. it provides the buit-in iterative methods also like forEach(), entries()



        HTMLCollection:

         1. represnts the collection of HTML elements
         2. HTMLCollection is returned by getelementByClassName()
         3. it never provides you the iterative methods

 */
