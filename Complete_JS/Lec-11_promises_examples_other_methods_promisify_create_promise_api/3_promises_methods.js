/**

   promise methods:

     - resolve(value/data) -> success scenario
     - reject(err) -> failure scenario
     - then(scb, fcb): then is handling resolved value it could be a sucess value or failure value as well
     - catch(fcb) ->  catch is handling the error value
     - finally(cb) -> will invoke always whether the promised is getting resolved or rejected.

 */
